"""
ИНСТРУКЦИЯ ПО СКАЧИВАНИЮ И DEPLOYMENT
=====================================

Полная пошаговая инструкция для скачивания всех файлов и развёртывания
на Railway как Telegram бота.

DietaryApp Telegram Bot v1.0.0
Author: DietaryApp Team
License: MIT
"""

# ============================================================================
# 📥 ШАГИ СКАЧИВАНИЯ ФАЙЛОВ
# ============================================================================

## Вариант 1: Через GitHub (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Клонируем репозиторий (содержит все файлы)
git clone https://github.com/dietaryapp/telegram-bot.git
cd telegram-bot

# 2. Структура файлов автоматически правильная:
# telegram-bot/
# ├── main.py
# ├── config.py
# ├── database.py
# ├── handlers.py
# ├── middleware.py
# ├── requirements.txt
# ├── Dockerfile
# ├── railway.json
# ├── .env.example
# ├── README.md
# ├── services/
# │   ├── __init__.py
# │   ├── recipe_service.py
# │   ├── gpt_service.py
# │   └── shop_service.py
# └── logs/ (создаётся автоматически)

# 3. Готово к развёртыванию!
```

## Вариант 2: Ручная загрузка файлов

Скачайте файлы вручную в следующем порядке:

### Корневые файлы:
1. main.py
2. config.py
3. database.py
4. handlers.py
5. middleware.py
6. requirements.txt
7. Dockerfile
8. railway.json
9. .env.example

### Папка services/:
Создайте папку `services` и положите:
1. services/__init__.py
2. services/recipe_service.py
3. services/gpt_service.py
4. services/shop_service.py

---

# ============================================================================
# ⚙️ ЛОКАЛЬНАЯ КОНФИГУРАЦИЯ
# ============================================================================

## Шаг 1: Создание .env файла

```bash
# Копируем пример конфигурации
cp .env.example .env

# Открываем в редакторе
nano .env

# Заполняем значения:
# - TELEGRAM_BOT_TOKEN (от @botfather в Telegram)
# - OPENAI_API_KEY (от platform.openai.com)
# - GOOGLE_MAPS_API_KEY (от console.cloud.google.com)
# - DATABASE_URL (для локального: postgresql://user:pass@localhost:5432/db)
```

## Шаг 2: Установка зависимостей

```bash
# Создаём виртуальное окружение
python3 -m venv venv

# Активируем (Linux/Mac)
source venv/bin/activate

# Активируем (Windows)
venv\Scripts\activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Проверяем
python -c "import telebot; print('✅ pytelegrambotapi установлена')"
```

## Шаг 3: Тестирование локально

```bash
python main.py

# Если всё работает, увидите:
# 🎯 DietaryApp v1.0.0 запускается | Окружение: development
# ✅ Telegram бот инициализирован: 1234567890:AAHd...
# 🗄️  Инициализация базы данных PostgreSQL...
# ✅ База данных инициализирована
# ✅ Middleware инициализирована
# ✅ Все обработчики зарегистрированы
# 🚀 Запуск DietaryApp Telegram Bot в режиме POLLING...
```

---

# ============================================================================
# 🚂 DEPLOYMENT НА RAILWAY
# ============================================================================

## ВАРИАНТ A: Через GitHub (Рекомендуется - Auto-Deploy)

### Шаг 1: Подготовка GitHub репозитория

```bash
# 1. Создаёте новый репозиторий на GitHub (используя + → New repository)

# 2. Клонируете его локально (если ещё нет)
git clone https://github.com/YOUR_USERNAME/telegram-bot.git
cd telegram-bot

# 3. Копируете все файлы DietaryApp в папку
# (из предыдущего скачивания)

# 4. Инициализируете Git (если не сделано)
git init
git add .
git commit -m "Initial commit: DietaryApp Telegram Bot"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot.git
git push -u origin main
```

### Шаг 2: Создание проекта на Railway

```
1. Откройте https://railway.app
2. Нажмите "Sign in with GitHub" → авторизуйтесь
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий telegram-bot
6. Railway автоматически прочитает railway.json и Dockerfile
```

### Шаг 3: Добавление PostgreSQL

```
1. В Railway Dashboard нажмите "+ Add"
2. Выберите "Database" → "PostgreSQL"
3. Railway автоматически:
   - Создаст БД
   - Установит переменную DATABASE_URL
   - Свяжет с приложением
```

### Шаг 4: Установка переменных окружения

В Railway Dashboard (Telegram Bot → Variables):

```
TELEGRAM_BOT_TOKEN = YOUR_TOKEN_FROM_BOTFATHER
OPENAI_API_KEY = sk-YOUR_API_KEY_FROM_OPENAI
GOOGLE_MAPS_API_KEY = YOUR_KEY_FROM_GOOGLE_CLOUD
ENVIRONMENT = production
DEBUG = False
LOG_LEVEL = INFO

(DATABASE_URL будет автоматически добавлена)
```

### Шаг 5: Запуск

```
1. Нажмите Deploy или (красная кнопка сверху)
2. Ждём 2-3 минуты пока Railway:
   - Клонирует репозиторий
   - Строит Docker образ
   - Запускает контейнер
3. Смотрим Logs → должны видеть:
   ✅ Telegram бот инициализирован
   ✅ База данных инициализирована
   🚀 Запуск...
```

---

## ВАРИАНТ B: Через Railway CLI (Для продвинутых)

### Установка Railway CLI

```bash
# macOS
npm i -g @railway/cli

# Linux
npm i -g @railway/cli

# Windows (PowerShell)
npm i -g @railway/cli

# Или используя pip (Python)
pip install railway
```

### Deployment через CLI

```bash
# 1. Логин в Railway
railway login

# 2. Создание нового проекта
railway init

# 3. Выбираем "Node/Python"
# 4. Даём имя проекта: telegram-bot

# 5. Добавляем PostgreSQL
railway add

# 6. Выбираем PostgreSQL

# 7. Устанавливаем переменные
railway variables set TELEGRAM_BOT_TOKEN=YOUR_TOKEN
railway variables set OPENAI_API_KEY=sk-YOUR_KEY
railway variables set GOOGLE_MAPS_API_KEY=YOUR_KEY

# 8. Запускаем
railway up

# 9. Смотрим логи
railway logs --tail

# 10. Открываем сервис
railway open
```

---

# ============================================================================
# ✅ ПРОВЕРКА УСПЕШНОГО DEPLOYMENT
# ============================================================================

## Как убедиться, что бот работает?

### 1. Проверка логов на Railway

```
1. Откройте Dashboard → Telegram Bot Service
2. Перейдите на вкладку "Logs"
3. Должны видеть:
   ✅ Telegram бот инициализирован
   ✅ База данных инициализирована
   🚀 Запуск DietaryApp Telegram Bot...
```

### 2. Тестирование через Telegram

```
1. Откройте Telegram
2. Найдите вашего бота (по имени из @botfather)
3. Отправьте /start
4. Если работает, должны увидеть:
   - Приветственное сообщение
   - Главное меню с кнопками
   - "🔍 Поиск рецепта"
   - "📍 Магазины рядом"
   - "📔 Мой дневник"
   - "🤖 Спросить диетолога"
   - "⚙️ Настройки"
```

### 3. Проверка команд

```
Попробуйте команды:
/start  → показывает главное меню
/help   → показывает справку
/status → показывает статус (в разработке)
```

---

# ============================================================================
# 🚨 ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ
# ============================================================================

### Ошибка: "Module 'services' not found"

**Решение:**
```
1. Убедитесь, что папка services/ на месте
2. Проверьте, что в папке есть __init__.py
3. Структура должна быть:
   services/
   ├── __init__.py
   ├── recipe_service.py
   ├── gpt_service.py
   └── shop_service.py
```

### Ошибка: "TELEGRAM_BOT_TOKEN not set"

**Решение:**
```
1. На Railway Dashboard установите переменную:
   TELEGRAM_BOT_TOKEN = YOUR_TOKEN
2. Нажмите Deploy для перезагрузки
3. Проверьте логи
```

### Ошибка: "Connection to database failed"

**Решение:**
```
1. Убедитесь, что PostgreSQL добавлена в проект
2. Railway должна автоматически установить DATABASE_URL
3. Если нет, добавьте вручную (формат вы найдёте в PostgreSQL Dashboard)
4. Нажмите Redeploy
```

### Ошибка: "OpenAI API error"

**Решение:**
```
1. Проверьте, что OPENAI_API_KEY правильный
2. Убедитесь, что на аккаунте есть баланс
3. Проверьте лимиты использования
4. Используйте gpt-3.5-turbo вместо gpt-4 для экономии:
   models/gpt_service.py:
   model="gpt-3.5-turbo",
```

### Ошибка: "Bot doesn't respond in Telegram"

**Решение:**
```bash
# 1. Проверьте логи
railway logs --tail

# 2. Убедитесь, что контейнер запущен
railway ps

# 3. Если контейнер упал, посмотрите ошибки
railway logs --since 1h

# 4. Перезагрузите контейнер
railway restart

# 5. Проверьте все переменные окружения
railway variables
```

---

# ============================================================================
# 🔄 ОБНОВЛЕНИЕ КОДА
# ============================================================================

## Вариант A: GitHub Auto-Deploy (Рекомендуется)

```bash
# 1. Делаете изменения локально
nano main.py  # например

# 2. Коммитите и пушите
git add .
git commit -m "Update: new features"
git push origin main

# 3. Railway автоматически:
#    - Замечает push
#    - Собирает новый Docker образ
#    - Перезагружает приложение
#    - Вы видите это в Dashboard → Deployments
```

## Вариант B: Через Railway Dashboard

```
1. Откройте Repository Settings в Railway
2. Выключите Auto-Deploy (если хотите контролировать)
3. Когда готовы к обновлению:
   - Откройте Deployments
   - Нажмите "New Deploy"
   - Railway пересоберёт контейнер
```

## Откат на предыдущую версию

```
1. Railway Dashboard → Deployments
2. Найдите нужную версию в истории
3. Нажмите "Rollback"
4. Railway вернёт приложение на предыдущее состояние
```

---

# ============================================================================
# 💡 СОВЕТЫ И ЛУЧШИЕ ПРАКТИКИ
# ============================================================================

### 1. Логирование в продакшене
```python
# Всегда используйте logger вместо print()
from loguru import logger
logger.info("Что-то произошло")  # видно в Railway логах
```

### 2. Обработка ошибок
```python
# Оборачивайте API вызовы
try:
    response = gpt_service.ask_dietician(question)
except Exception as exc:
    logger.error(f"Error: {exc}", exc_info=True)
    return "Извините, ошибка"
```

### 3. Мониторинг
- Регулярно проверяйте Logs в Railway Dashboard
- Смотрите Metrics (CPU, память, сеть)
- Установите alerts если нужно

### 4. Безопасность
- Никогда не коммитьте .env файл с реальными ключами
- Используйте .gitignore для .env
- Все ключи храните только в Railway Variables

### 5. Performance
- Используйте кэширование результатов
- Ограничивайте количество результатов (LIMIT)
- Оптимизируйте SQL запросы

---

# ============================================================================
# 📞 ПОДДЕРЖКА И РЕСУРСЫ
# ============================================================================

## Документация
- **Railway docs**: https://docs.railway.app
- **Telegram Bot API**: https://core.telegram.org/bots/api
- **OpenAI API**: https://platform.openai.com/docs
- **SQLAlchemy**: https://docs.sqlalchemy.org

## Сообщество
- **Railway Discord**: https://discord.gg/railway
- **Telegram Bot SDK**: https://pypi.org/project/pytelegrambotapi/
- **Stack Overflow**: #telegram-bot, #railway-app

## Помощь
- 📧 support@dietaryapp.com
- 💬 Telegram: @dietaryapp_ru
- 🐛 GitHub Issues: https://github.com/dietaryapp/telegram-bot/issues

---

## 🎉 ГОТОВО!

Поздравляем! Ваш Telegram бот успешно развёрнут на Railway. 

Теперь вы можете:
✅ Добавлять рецепты для пользователей
✅ Интегрировать реальные API (вместо mock данных)
✅ Масштабировать приложение
✅ Добавлять новые фичи

Хорошей разработки! 🚀
"""